export default class Customer {
    id: string;
    firstName: string;
    lastName: string;
    phoneNumber?: number;
    fullName: string;
    constructor(id: string, firstName: string | undefined, lastName: string | undefined, phoneNumber: number);
}
